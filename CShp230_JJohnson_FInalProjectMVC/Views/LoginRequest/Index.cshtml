@model IEnumerable<CShp230_JJohnson_FInalProjectMVC.Models.vLoginRequest>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmailAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LoginName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NewOrReactivate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReasonForAccess)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateRequiredBy)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmailAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LoginName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NewOrReactivate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReasonForAccess)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateRequiredBy)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.LoginId }) |
                @Html.ActionLink("Details", "Details", new { id=item.LoginId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.LoginId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
